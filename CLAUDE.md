# 루프체크 (LoopCheck)

게임 팀을 위한 지표 설계기 + 진단 리포트. 원티드 AI 챔피언십 2026 출품작이자 1인 창업 제품의 1버전.

## 마감

- 참가 접수 **2026-09-18**
- 과제 제출 **2026-09-20**
- 심사 기간 2026-09-21 - 10-17 (이 기간 내내 배포 링크가 살아 있어야 함)

**9/17 이후로는 기능을 추가하지 않는다. 깎기만 한다.**

## 제품 한 줄

> 기존 분석 도구는 이미 데이터가 있는 팀을 전제한다.
> 루프체크는 무엇을 쌓을지 모르는 단계에서 시작해, 쌓인 다음에는 한국어로 무엇을 먼저 고쳐야 하는지까지 이어 준다.

- **입구(무료)**: 장르와 핵심 루프 → 로그 이벤트 스키마, KPI 정의서, A/B 설계안, SQL DDL
- **출구(유료 예정)**: CSV 업로드 → 리텐션·퍼널·세그먼트 진단 + 우선순위 개선안

## 지금 상태

| 항목 | 상태 |
|---|---|
| Next.js 16 (App Router, TS, Turbopack) 스캐폴드 | 됨 |
| 디자인 토큰 `app/globals.css` | 됨 |
| 랜딩 `app/page.tsx` | 됨 |
| 설계기 `app/design/page.tsx` + `ResultView.tsx` | 됨 (프리셋 4종 기반) |
| 프리셋 JSON `public/presets/*.json` | 됨 (puzzle, idle, gacha-rpg, roguelike) |
| 자유 입력 → LLM 호출 `app/api/design` + `lib/llm/designer.ts` | 됨 (Gemini). 프롬프트 원본 그대로, 형식 검증 실패 시 1회 재시도. 키 없으면 프리셋 안내 |
| 진단 엔진 `lib/diagnose/engine.ts` + 해석 규칙 `findings.ts` | 됨. `npm run check:diagnose` 40/40 (시그널 5개·광고 함정·업로드·숫자 근거 검사 포함) |
| 진단 리포트 `app/diagnose` | 됨 (샘플 데모 `/diagnose?sample` + 업로드) |
| 진단 AI 해석 `app/api/interpret` + `lib/diagnose/interpret.ts` | 됨 (Gemini). 사실표에 없는 숫자가 나오면 거부·재시도, 실패 시 규칙 해석 유지 |
| 진단 CSV 업로드 `lib/diagnose/schema.ts` | 됨. 헤더로 테이블 인식, 별칭 자동 매핑 + 수동 매핑, 유저·세션만 필수 |
| 배포 | 됨 — https://loopcheck-lac.vercel.app (GitHub `vanillapapaya/loopcheck` main push 시 자동 배포) |

## 남은 작업 (우선순위)

1. ~~`npm install` 다시 돌리고 `npm run build` 통과시키기.~~ **됨 (2026-09-15).** 아래 "알려진 문제" 참조.
2. ~~배포. Vercel + GitHub 연동.~~ **됨 (2026-09-15).** https://loopcheck-lac.vercel.app — 저장소는 public, 심사 종료(10/17) 후 private 전환 예정.
3. ~~진단 기능.~~ **됨 (2026-09-15).** 데모·업로드·AI 해석. 결제 퍼널은 샘플에 노출·클릭 로그가 없어 계산하지 않고 리포트에 그 사실을 적는다(결정 2026-09-15).
4. ~~자유 입력 설계기.~~ **됨 (2026-09-15).** 실모델(`gemini-3.8-flash`) 확인 완료:
   - 설계기 합격선 5/6 (1번 "result 컬럼"은 level_win/fail/abandon 분리 이벤트로 대체해 실질 충족), 응답 약 20초
   - 진단 해석: 정답지 결론 전부 도달(L12 충돌, continue_1 1,200원, 광고 3-4회 정점·7회 이상 표본 39 보류, 저사양, tiktok), 지어낸 숫자 0, 응답 7-20초
   - Vercel 환경변수 설정·Redeploy 완료(2026-09-15). 배포 사이트에서 설계기·진단 해석 모두 `gemini-3.8-flash`로 응답 확인
   - 키가 대화에 노출됐으므로 심사 종료 후 폐기·재발급할 것
5. 연락 경로. 랜딩 푸터의 `[연락처 미정]`을 실제 값으로 교체. 심사 기간 한 달이 리드 수집 창이다.

## 알려진 문제

**해결됨 (2026-09-15).** 실제 원인은 Node 버전이었다. 로컬 Node가 v18이었는데 Next 16은 `>=20.9.0`을 요구한다.
Node 22(nvm)로 바꾸고 클린 재설치하니 빌드가 통과했다. `.nvmrc`(22)와 `package.json`의 `engines.node`로 버전을 고정해 두었다.
작업 전에 `nvm use`를 먼저 실행할 것. 아래는 원래 기록이다.

빌드 시 `error TS7016: Could not find a declaration file for module 'next/link'` 등이 무더기로 난다.
원인은 코드가 아니라 설치다. `node_modules/.bin`이 비어 있고 next 타입 재export가 깨져 있다 —
원격 마운트 파일시스템에서 npm이 심볼릭 링크를 못 만든 탓으로 보인다.

**로컬 터미널에서 `rm -rf node_modules package-lock.json && npm install`** 하면 대부분 해결된다.
그래도 남으면 `tsconfig.json`에 `"skipLibCheck": true`가 있는지 확인한다.

## LLM (Gemini)

- 키: `GEMINI_API_KEY` (Vercel 환경변수 / 로컬 `.env.local`, 예시는 `.env.example`). 모델은 `GEMINI_MODEL`로 바꿀 수 있다.
- 호출은 `lib/llm/gemini.ts` 한 곳. SDK 없이 REST. 404면 다음 모델로, 429·키 없음은 `LlmUnavailable`로 던져 호출부가 프리셋·규칙 해석으로 물러난다.
- 진단 해석은 브라우저가 만든 **사실표**(집계 수치 문장, `buildFacts`)만 서버로 보낸다. 출력에 사실표에 없는 숫자가 있으면 거부한다(10 이하 정수만 예외). 원칙 1을 코드로 강제하는 지점이니 느슨하게 만들지 말 것.
- 공개 링크라 IP당 호출 제한(`allowRequest`)과 같은 사실표 캐시가 있다. 둘 다 인스턴스 메모리 기준이다.

## 설계 원칙 (바꾸지 말 것)

1. **통계는 코드가 계산하고 LLM은 해석과 가설 생성만 한다.** LLM에게 숫자를 계산시키지 않는다.
   단, 이 분리는 마케팅 차별점이 아니다(Amplitude가 무료로 한다). 품질 근거로만 쓴다.
2. **업로드한 데이터는 저장하지 않는다.** 진단 계산은 브라우저에서 수행하고, 서버로는 집계된 지표(작은 JSON)만 보낸다.
   개별 유저 행은 절대 서버로 보내지 않는다. 이건 카피가 아니라 실제 구현이어야 한다.
3. **운영비 0.** Vercel 무료 티어 안에서 돈다. GPU 상시 인스턴스 금지. 프리셋은 정적 파일이라 API 호출이 없다.
4. **프리셋 우선.** API 키가 없거나 rate limit에 걸려도 데모는 항상 돌아야 한다.

## 문체 규칙

분석 문장(리포트 요약·개선안·근거)은 `../prompts/voice.md`를 따른다. 원본을 고치면 아래 세 곳을 같이 고친다.

- `lib/diagnose/interpret.ts`의 `INTERPRET_SYSTEM` — voice.md 2장 주입 블록을 그대로 넣었다. 요약하거나 다시 쓰지 않는다
- `lib/diagnose/findings.ts` — 규칙 기반 문장. 요약·개선안 본문은 서술형, 근거와 실행 방법은 명사형 개조식
- `lib/voice/lint.ts` — voice.md 4-1장 기계 검사. AI 출력에서 fail이 나오면 거부하고 재시도시킨다

핵심만 추리면: 값에는 비교 대상과 차이를 붙이고, 비율의 차이는 %p로 쓰고, 첫 발견은 한 번 의심하고(4단 논증),
표본이 작으면 n을 밝히고 결론 대신 단서를 단다. 과장어·이모지·물결표 범위·지어낸 업계 평균은 쓰지 않는다.

## 디자인 규칙

`app/globals.css`의 CSS 변수를 쓴다. 새 색을 만들지 않는다.

- 서체: IBM Plex Sans KR (본문) + IBM Plex Mono (숫자·코드·이벤트명). `layout.tsx`에서 link로 로드
- 바탕 `--bg` 따뜻한 종이색, 먹색 `--ink` 하나, 신호색은 발견·경고에만
- 인라인 스타일을 쓰고 있다. Tailwind 도입하지 말 것 (의존성과 빌드 시간)
- 숫자는 `className="mono"` (tabular-nums 포함)
- 차트 색: 시리즈 `--series` #2a78d6, 위험 `--danger` #D03B3B, 주의 `--warn` #EC835A
  - 2계열 이상이면 범례 필수, 단일 계열이면 범례 없음
  - 상태색은 아이콘이나 직접 라벨과 함께 쓴다. 색만으로 의미를 전달하지 않는다

## 진단 엔진 설계

샘플 데이터: `../data/*.csv` (6,000유저 30일치, seed 20260914 고정, `../data/generate.py`로 재현)
정답지: `../02-sample-data-spec.md` — **심어둔 시그널 5개와 리포트가 도달해야 할 결론이 적혀 있다. 반드시 읽을 것.**

계산은 전부 브라우저에서:

| 지표 | 정의 |
|---|---|
| 리텐션 | classic N-day. rolling과 혼용 금지 |
| 레벨별 클리어율 | 시도 대비. 도달자 대비와 **함께** 보여준다. 둘이 벌어지는 레벨이 재도전 구간 |
| 레벨 이탈 | 레벨 N 도달자 중 N+1로 못 간 비율 |
| 결제 퍼널 | 노출 → 클릭 → 구매. 분모를 반드시 라벨로 명시 |
| 세그먼트 | 채널, 기기 등급, 플랫폼별 D1/D7 비교 |

**반드시 통과해야 하는 함정**: 광고 노출량 대비 잔존을 누적으로 집계하면 "많이 볼수록 잔존이 높다"(31회 이상 = D7 100%)가 나온다. 이건 생존 편향이라 **틀렸다**. day_n을 고정하고 당일 노출량 대비 익일 잔존으로 봐야 역U자(3-4회 정점, 7회 이상 역전)가 드러난다. 그리고 7회 이상 구간은 표본이 39개뿐이므로 결론 대신 단서를 달아야 한다.

이 함정을 통과하지 못하는 엔진은 출시하지 않는다.

## 참고 문서

- 옵시디언: `~/Documents/vanillapapaya/Project/Active/AI-챔피언십-2026/`
  - `00-인덱스` 전체 지도 / `03-문제-정의` / `04-샘플데이터-정답지` / `07-시장-전략` / `06-6일-실행계획`
- 프롬프트 원본: `../prompts/designer.v1.md`
- **문체 규칙: `../prompts/voice.md`** — 진단 리포트가 내놓는 모든 문장은 이 규칙을 따른다.
  실제 분석 문서(에픽세븐 효과분석 종합 시트)에서 추출했다. 쓰는 법:
  1. 2장의 주입 블록을 리포트 생성 시스템 프롬프트에 **그대로** 붙인다 (요약하지 말 것)
  2. 3장의 예시 쌍을 few-shot으로 함께 넣는다
  3. 4-1의 기계 검사 항목은 린터로 만들어 생성 결과를 자동 채점한다
- UI 목업: Claude 아티팩트 "루프체크 제품 화면" (5화면 — 랜딩/설계기 입력/설계기 결과/업로드/리포트)

## 서비스 이름

"루프체크"는 임시다. 핵심 루프를 점검한다는 뜻으로 잡았다. 바꿔도 된다.
