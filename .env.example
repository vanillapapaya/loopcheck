# 로컬: 이 파일을 .env.local로 복사해 값을 채운다. 배포: Vercel 프로젝트 Settings → Environment Variables에 같은 이름으로 넣는다.
# 키가 없으면 설계기는 프리셋만, 진단 리포트는 규칙 기반 해석만 보여준다.
GEMINI_API_KEY=
# 선택. 비우면 gemini-3.8-flash → gemini-3.5-flash → gemini-2.5-flash 순으로 시도한다.
GEMINI_MODEL=
